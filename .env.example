# AWS Configuration
AWS_REGION=us-east-1
AWS_PROFILE=default

# Flask Configuration
FLASK_DEBUG=True
FLASK_HOST=0.0.0.0
FLASK_PORT=5000

# Optional: AWS Credentials (if not using AWS CLI or IAM roles)
# AWS_ACCESS_KEY_ID=your_access_key_here
# AWS_SECRET_ACCESS_KEY=your_secret_key_here